node_modules/
dist/
coverage/

.env
.env.*
!/.env.example
!apps/api/.env.test.example

.DS_Store
Thumbs.db

apps/api/dist/
apps/api/node_modules/

prisma/dev.db
.local-postgres/
data/
nginx/certbot/

# logs
*.log

apps/*/pnpm-lock.yaml

# IDE
.vscode/

package-lock.json

apps/mobile/.expo/
uploads/
apps/api/uploads/

# Claude assessment artifacts
docs/*.pdf
docs/*.py

# Local APK archive — ~60MB per build, never belongs in git history
apk-backups/

# OCR eval dataset — ~40MB of real Smart Sensor photos, kept on disk for
# decoder tuning but not versioned (binary bloat). Move to git-lfs if it ever
# needs to be shared across machines.
sample-image/
